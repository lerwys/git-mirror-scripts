Git mirroring scripts
=====================

Scripts for mirroring a git repository to another location

# Instructions

```bash
/scripts/setup-synchronize-git-mirror.sh -o <Origin repository URL> -t <Target repository URL> -p <Path to clone repository>
```
